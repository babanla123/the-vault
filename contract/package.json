{
  "name": "@the_vault/anchor",
  "version": "0.0.1",
  "description": "Anchor contract for The Vault - Digital Asset Management",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "anchor build",
    "test": "anchor test",
    "deploy": "anchor deploy",
    "keys": "anchor keys list"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.30.0",
    "@solana/web3.js": "^1.95.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-mocha": "^10.0.0",
    "typescript": "^5.0.0"
  }
}
